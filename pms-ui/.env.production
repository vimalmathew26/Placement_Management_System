# MongoDB Connection (same as backend DATABASE_URI)
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster-url>/"

# Backend API URL for production
NEXT_PUBLIC_API_BASE_URL="<your-production-api-url>"